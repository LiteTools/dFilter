<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>WinExe</OutputType>
    <TargetFramework>net5.0-windows</TargetFramework>
    <UseWindowsForms>true</UseWindowsForms>
    <Version>0.2.0</Version>
    <Company>LiteTools</Company>
    <Authors>Matty</Authors>
    <Description>Have too many installation executables and can't filter all of them out? dFilter is the solution for this. dFilter is an application that cleans out all installer.exe / setup.exe files in a selected directory.</Description>
    <Copyright>(c) LiteTools 2022</Copyright>
  </PropertyGroup>

</Project>